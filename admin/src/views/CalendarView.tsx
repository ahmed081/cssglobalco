import { useAppSelector } from '../store/hooks';import { Card,Empty } from '../components/ui';
export function CalendarView(){const cands=useAppSelector(s=>s.recruit.candidates.filter(c=>c.interviewDate));return <div className="dash-grid"><Card className="full-card"><h3>Interview Schedule</h3>{!cands.length?<Empty icon="🗓" title="No interviews" text="Scheduled interviews will appear here."/>:cands.map(c=><div className="event-row" key={c.id}><div className="event-date">{c.interviewDate}</div><div><b>{c.fn} {c.ln}</b><p>{c.role} · Recruiter: {c.recruiter}</p></div><button>Join / Details</button></div>)}</Card><Card><h3>Reminders</h3><div className="mini-row"><span>Follow up offers</span><b>2</b></div><div className="mini-row"><span>Missing CVs</span><b>1</b></div></Card></div>}
